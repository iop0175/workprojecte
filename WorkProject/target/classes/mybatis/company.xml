<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="company">

	<select id="cityCount" resultType="Integer">
		SELECT
		(SELECT COUNT(*) FROM
		COMWORK WHERE WORK_REGION = #{cityName}) +
		(SELECT COUNT(*) FROM
		VIP_COMWORK WHERE WORK_REGION = #{cityName}) as cityCount
		FROM dual
	</select>
	<select id="cityList" resultType="work">
		SELECT *
		FROM (
		SELECT *
		FROM comwork
		WHERE WORK_REGION = #{cityName}
		ORDER BY SYS.DBMS_RANDOM.RANDOM()
		)
		WHERE ROWNUM &lt;= 10
	</select>
	<select id="vipcityList" resultType="vipwork">
		SELECT *
		FROM (
		SELECT *
		FROM VIP_COMWORK
		WHERE WORK_REGION = #{vipCityName}
		ORDER BY SYS.DBMS_RANDOM.RANDOM()
		)
		WHERE ROWNUM &lt;= 10
	</select>
</mapper>